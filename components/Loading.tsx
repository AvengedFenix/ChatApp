import React from 'react';
import {Text} from 'react-native';

const Loading = () => {
  return (
    <Text
      style={{
        alignSelf: 'center',
        fontSize: 100,
        marginVertical: '50%',
      }}>
      ...
    </Text>
  );
};

export default Loading;
